[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "scribe"
version = "0.1.0"
description = "STT-free meeting scribe"
requires-python = ">=3.10"

[project.scripts]
scribe = "scribe.cli:main"
