# Log

Reader: anyone tracing why the server is the way it is. Newest first.

## 2026-09-18 — built from glm-subagent; five lanes, router, telemetry

Goal: `goals/2026-09-18-unified-subagent.md`. Coordinator: Claude Code (conductor). Build lanes: GLM planned; GLM hit a z.ai 1313 account-level limit on the first two units, so every unit ran on Claude agents (8 of the 8-agent cap).

UAT at close:

| Row | Result |
|---|---|
| 1.1 | PASS (465 tests) |
| 1.2 | PASS |
| 1.3 | PASS |
| 2.1–2.4 | PASS (router, lane_state, health tests; no start-llm in src) |
| 3.1 | PASS (fake codex) |
| 3.2 | DEFERRED to 2026-09-20 13:29 |
| 4.1 | PASS |
| 4.2 | PASS (sampling fields sent: none) |
| 4.3 | PASS |
| 4.4 | bppc PASS; deepseek DEFERRED (402 Insufficient Balance) |
| 5.1–5.3 | PASS |
| 5.4 | PASS: net saving glm $711.40, deepseek $172.82, bppc $0.68 (`data/cost-2026-09-18/`) |
| 6.1 | PASS (max_agents 1, decisions S7) |
| R.1 | PASS (3 HIGH fixed; open items carry reasons) |
| R.2 | PASS (no remote; nothing pushed) |
| R.3 | PASS |
| R.4 | PASS |

### Run ledger

| Time (UTC) | Unit | Lane | Event |
|---|---|---|---|
| 06:14 | setup | coordinator | S created from `git archive` of G 96933a2 (751e783); Codex fixtures staged; oMLX tool_use probe PASS (607a9dd) |
| 06:15 | U1 | GLM a1 | dispatched run-148d7b3983c6 |
| 06:20 | U5b | GLM a2 | dispatched run-e43668a13707 in worktree cost-join |
| 06:44 | U1 | GLM | FAILED rate_limited 1313 after 1763 s. The z.ai text asks the account holder to file a request to regain access: an account-level fair-use limit, not a short throttle. No edits on disk. |
| 06:45 | U5b | GLM | cancelled after 1390 s; only scripts/pricing.toml written |
| 06:45 | U1, U5b | fork decided | GLM closed for this run (1313; three children on one plan, one from the e-launcher session). Rerouted to Claude Agent, the lane plan's fallback |
| 06:45 | telemetry | finding | Both GLM runs reported usage 0 despite real work (a2: 42095 activity events, 6 steps). Schema 3 must count tokens on failed and cancelled runs; fed into U5 |
| 06:52 | U5b | Claude Agent | done 964969a on cost-join. Coordinator UAT: 12 tests pass; real-data run exit 0 in 2.4 s, 735 runs, 112 unmatched. glm cf $1103.82 vs parent $311.87; deepseek $231.75 vs $28.88. Net saving null until GLM plan price and DeepSeek rates are in pricing.toml. Parent messages that issued calls for several runs are split evenly (agent's decision, kept; unsplit column retained) |
| 06:58 | U1 | Claude Agent | done 7f57188. Coordinator UAT: 248 passed; no glm_subagent_mcp/GSA_ left; no "--bare" outside comments; test_lanes 17 passed. Accepted deviations: global SAM_MODEL/SAM_BASE_URL not applied across lanes; all model aliases map to the run's model; ANTHROPIC_AUTH_TOKEN no longer a GLM key source. README still GLM-era (R.3 rewrites it) |
| 07:00 | merge | coordinator | cost-join merged c8f9188 (uv.lock regenerated); 260 passed with the analysis group |
| 07:01 | U2 | Claude Agent | dispatched on main; brief file sent by message after the prompt went out truncated |
| 07:02 | U3 | Claude Agent | dispatched in worktree codex-driver; guard hook in a per-agent CODEX_HOME plus workspace-write sandbox (decided forward: codex-cli has stable PreToolUse hooks) |
| 07:10 | bppc | coordinator | Gary: bppc on. LAN SSH ok, Tailscale route down. Coordinator ran start-llm (GPU idle before); :8080 health ok, :8081/slots 200, :8081/metrics 501 (no --metrics). 4.4 bppc now runnable |
| 07:21 | U3 | Claude Agent | done bce9ee3 on codex-driver. Coordinator UAT: 266 passed 1 skipped; test_codex_driver 19 passed; no bypass-sandbox flag in src. Found from the binary: Codex PreToolUse payload has Claude keys plus turn_id; Codex rejects permissionDecision allow, so allow = silent exit 0. Unverified until live (3.2): hooks load from custom CODEX_HOME, real tool_name values, resume flag order, symlinked auth. Accepted: input = input_tokens - cached_input_tokens |
| 07:35 | U2 | Claude Agent | done b2d01af. Coordinator UAT: 330 passed (analysis group); no start-llm in src; 71 router/lane_state/health cases PASSED by name; oMLX /api/status returns status ok (agent had not checked). Accepted: outcome "refused" + code field; outcomes unavailable and no_lane; no backoff on balance/usage-limit; cold load extends the run deadline; 1308/1310 without reset do not close. Risk for review: z.ai reset time read as local time |
| 07:37 | merge | fork decided | codex-driver into main conflicts in runs.py by design: the router walks lanes inside one Agent, the codex driver is an Agent subclass. Merge aborted; integration unit M1 dispatched to a Claude Agent (coordinator does not resolve design conflicts) |
| 07:50 | M1 | Claude Agent | done ce66ebd (merge). Coordinator UAT: 352 passed; 11 codex+router cases PASSED. Design: per-hop driver objects (ClaudeDriver, CodexDriver); first hop that runs binds lane/driver for continues. Branch is master, not main |
| 08:08 | U5 | Claude Agent | done 750218f. Coordinator UAT: 384 passed; probes present in telemetry.py. Usage-0 cause: tokens only taken from the final result event; now per assistant message, deduped. Accepted: --include-partial-messages added for real TTFT; retried and summary turns now counted. Open: codex tokens all-or-nothing (turn.completed only); admission only on delegate. bppc :8080 proxy reports backend stopped at 08:08 |
| 08:15 | U4/U6 | Claude Agent | dispatched (scripts: hook isolation port, smoke_lanes with oMLX logging proxy, bench_concurrency); added: bppc proxy cold start (backend stopped) treated like oMLX cold load |
| 08:18 | R.1 | Claude Agent | review dispatched on 750218f in parallel with U4/U6 (7th Claude agent of the 8 cap) |
| 08:40 | U4/U6 | Claude Agent | done 5279102. Coordinator UAT: 423 passed; hook_isolation_check PASS (a/b/c); smoke omlx PASS (deny Bash ~/.ssh/config, hello.txt, 4 turns 17196 tokens, 2 samples, run_summary, sampling fields sent: none) |
| 08:45 | 4.4 bppc | coordinator | smoke bppc FAIL twice (cold and warm): llama.cpp Qwen template raises "System message must be at the beginning" (HTTP 500) on Claude Code 2.1.276 requests; oMLX accepts them. Recorded as U-B1 HIGH in review.md |
| 08:50 | R.1 | Claude Agent | review done: 3 HIGH (H1 codex workdir, H2 ~/.codex and ~/.git-credentials readable, H3 agent ids shared across processes), 8 MED, 6 LOW. Saved to wiki/review.md with U-B1/U-B2 |
| 08:52 | F1 | Claude Agent | fix unit dispatched: U-B1, U-B2, H1–H3, M1, M3, M8, cheap LOWs (8th and last Claude agent of the cap) |
| 08:58 | 6.1 | coordinator | bench omlx 1/2/4/8 exit 0, 4 rows, 15 children all verified. Aggregate tok/s 20.1/18.3/7.6/8.2; p90 TTFT 3.9/10.5/26.5/181 s; swap 0. Rule chose max_agents=1; default change sent to F1. Data in wiki/data/ |
| 09:05 | 4.3 glm | coordinator | smoke glm PASS live: deny Bash ~/.ssh/config, hello.txt, 12315 tokens. The 1313 limit had cleared. Found U-T1: first turn record 0 tokens (usage only in stream deltas); sent to F1 |
| 09:08 | 4.4 deepseek | coordinator | smoke deepseek DEFERRED exit 3: live 402 Insufficient Balance classified deepseek_balance (first live proof of refusal classification) |
| 09:45 | F1 | Claude Agent | done 5294af8..197c681. Coordinator UAT: 465 passed; hook_isolation PASS; smoke bppc PASS (8940 tokens, deny ~/.ssh/config, run_summary VRAM 15762 MB, decode 94.4/78.5 tok/s); smoke omlx PASS; smoke glm PASS (turns 12132 = run 12132, U-T1 fixed live) |
| 09:50 | 5.4 | coordinator | cost_join on final code exit 0: 736 runs; glm cf $1103.89 vs parent $312.22 |
| 09:52 | R.4 | coordinator | ~/.claude.json backed up to ~/.claude.json.bak-2026-09-18; subagent added (keys copied from old entries, tuned knobs carried over); glm-subagent and qwen-subagent removed; deepseek-subagent kept. stdio probe: server "subagent", six tools |
| 10:25 | 5.4 | coordinator | DeepSeek peak rates from api-docs.deepseek.com (1.32/3.96/0.044 per 1M); GLM plan $80/month (Gary). Net saving glm $711.40 (737 runs incl. the fixture fix), deepseek $172.82. Test that read the real pricing file moved to a fixture by GLM (4c5cd5e; GLM lane open again); 466 passed |

## 2026-09-22/23 — fuse the delegation loop with the provider runtime

Goal: `goals/2026-09-22-fuse-delegation.md`. Plan: `~/.claude/plans/open-a-new-branch-atomic-tower.md`. Coordinator: Claude Code (conductor). Lanes: GLM and DeepSeek first (Gary 2026-09-22), Codex gpt-6-luna xhigh once its quota returned (Gary 2026-09-23), Claude agents only as fallback. Findings are named U-*; review findings live in `review.md`.

### Run ledger

| Time (UTC) | Unit | Lane | Event |
|---|---|---|---|
| 06:5x | P0 | coordinator | origin added, branch fuse from origin/main, master merged with unrelated histories (9d8ad71); goal file written |
| 07:0x | P0 | GLM a1 | dispatched run-21d0d5e679ec; transcript blank for 5 min while `list` showed guard allows; cancelled at 329 s, 8 steps, 43k tokens, no edits. Finding U-T2: `transcript` returns activity_count 0 during a run (351 at cancel) |
| 07:1x | P0 | Codex a2 | refused: usage limit until Sep 27 17:18; classified `usage_limit_after_work` with 0 output tokens and no tool call; lane not closed on disk. Finding U-R1 (fed into brief P1a addendum) |
| 07:1x | P0 | fork decided | rerouted to a Claude agent (worktree p0) |
| 07:2x | probe | coordinator | bppc :8080 down (LAN and Tailscale); oMLX up, models_loaded 0; grok CLI 1.0.34 installed, `-p` returns 402 "Grok Build usage balance exhausted" (fixture saved) |
| 07:26 | P0 | Claude agent | PASS a20cfc6: 481 tests; grep subagent_mcp empty; cli detect JSON; mcp import ok. Fast-forwarded to fuse, worktree removed. Agent ran install.sh once, repointing ~/.claude/skills/delegate, restored |
| 07:3x | deepseek | coordinator | Gary: DeepSeek back online. Lane reopened on disk; live probe PASS (4 s, 8.5k tokens, verified) |
| 07:3x | P1a | DeepSeek a4 | dispatched run-3042be26bc1f in worktree p1a (config, ProviderConfig, claude/codex providers, runs). Transcript again blank mid-run (U-T2 confirmed on a second lane) |
| 07:5x | P1a | DeepSeek a4 | cancelled at 297 s, 17 steps, 111k tokens, no edits. Root cause found in the child's own session log: every tool call returned "PreToolUse hook error: can't open file src/subagent_mcp/runtime/approval_hook.py". The running MCP server computed the hook path at import, before P0 moved the hook; the fast-forward of `fuse` in the checkout the server runs from removed the file, so every child call failed closed from that moment. Also the same cause for the GLM run being blank (a1 ran after the ff? no: a1 ran before the ff, so U-T2 stands on its own). Finding U-H1: the hook path must not depend on the checkout the server started from (copy the hook into the session root at startup, or restart the server after a layout change). The child also probed `~/.claude/settings.json` and `~/.claude.json` with no-op Edits while blocked; denied by the failed hook, but recorded (child behaviour under blanket denial). |
| 07:5x | fix | coordinator | untracked shim at `src/subagent_mcp/runtime/approval_hook.py` forwarding to `src/subagent/guard/approval_hook.py` (excluded via .git/info/exclude); hook probe on deepseek PASS (Read, Bash, Write, verified, 9 s) |
| 07:5x | P1a | DeepSeek a6 | re-dispatched run-41026a1c1690 in worktree p1a |
| 08:4x | P1a | DeepSeek a6 | FAILED `loop` after 2798 s, 263 steps, 11.3M tokens (10.2M cache reads), zero edits on disk, no commit. Finding U-D1: deepseek-v4-pro read the tree for 47 minutes and never edited; the loop killer fired on the 8th identical Read. Rerouted to a Claude agent (lane plan fallback). |
| 09:0x | rule | Gary | "Next time retry with GLM and DeepSeek": a unit that fails on one lane goes to GLM, then DeepSeek, before a Claude agent. Applies from P1b onward; P1a stays on the Claude agent already mid-edit. |
| 09:49 | P1a | Claude agent | PASS 6de7a5f (4 commits): 504 passed, 2 xfail (Tailscale resolver tests, replaced by resolve_cmd tests in test_health). Coordinator UAT: suite green, SAM_/lanes grep empty, /dev/null config raises "no providers configured", machine-literal grep empty. Decisions accepted: `api_key_default` per provider; Provider protocol gains `spawn`/`binary`; new finish_reason `refused`; session root now `~/.subagent/sessions`; child_env strips only declared key envs. Fast-forwarded to fuse; worktree removed |
| 09:5x | fix | coordinator | hook shim extended: maps SAM_APPROVAL_SOCKET/SAM_HOOK_TIMEOUT to the SUBAGENT_* names the new hook reads (the running server still sets the old names) |
| 09:5x | P1b/P1d/P1e | GLM a8 / DeepSeek a9 / GLM a10 | dispatched in worktrees p1b, p1d, p1e from 6de7a5f (Gary's rule: GLM and DeepSeek before Claude agents) |
| 10:0x | P1b/P1d/P1e | GLM/DeepSeek/GLM | all three BLOCKED in under 80 s: every child tool call denied "SUBAGENT_APPROVAL_SOCKET is not set". Cause: my shim rewrite (SAM_* → SUBAGENT_* mapping) sat behind a failed `git worktree remove` in a `&&` chain and never ran; the old shim forwarded to the renamed hook without the mapping. All three children reported BLOCKED correctly and did not try to bypass the gate. Also: the old in-memory guard blocks `pytest -p no:cacheprovider` as a verification command ("loads code or config from elsewhere"); verification changed to plain `uv run pytest -q --ignore=…`. Finding U-H1 stands: hook path and env names must not depend on the checkout the server started from. |
| 10:0x | P1b/P1d/P1e | same agents | shim fixed and tested (unreachable-socket path now reported, not "not set"); the three agents continued in their sessions with the corrected verification |
| 10:0x | P1b | GLM a8 | z.ai 1313 on every request once two GLM children ran at once (same pattern as 09-18); a8 made no edits. Cancelled; P1b re-dispatched on DeepSeek. P1e (a10) stays alone on GLM and is progressing. Rule for the rest of the run: one GLM child at a time. |
| 10:1x | P1e | GLM a10 | 10 × 1313 api_error even as the only GLM child, no edits after 255 session lines. Cancelled; P1e re-dispatched on DeepSeek (a12). GLM struck for the rest of the run unless a later probe clears it. P1d (DeepSeek a9) editing: 5 files changed. |
| 10:09 | P1d | DeepSeek a9 | PASS cb26c8b after 890 s, 44 steps, 3.4M tokens (3.3M cache reads). Coordinator UAT: 119 targeted tests pass, full suite 508 passed 0 xfail (verified by the server), machine-literal grep empty. Fast-forwarded to fuse. Note: the child signed the commit "Claude Code" not "Claude Fable 5.1"; left as is. |
| 10:1x | P4b/P4c | DeepSeek | dispatched early (independent of P1/P2: new files under bench/ and docs/ only) in worktrees p4b, p4c from cb26c8b. Four DeepSeek children live (P1b, P1e, P4b, P4c) = the server's max_agents. |
| 10:23 | P1b | DeepSeek a11 | PASS 3b171c6 after 1323 s, 92 steps, 5.8M tokens (5.6M cache reads): copilot, grok, gemini providers + Usage.credits. Merged into fuse (clean merge with P1d). Open: gemini fixture synthetic (experimental gate); copilot session id minted twice (boot id discarded on first spawn). |
| 10:3x | P1e | DeepSeek a12 | PASS ece4a4b after 1704 s, 96 steps, 6.8M tokens: init/doctor/examples/core.py/PROVIDER_HOMES. Coordinator UAT: all 8 examples pass `doctor --no-probe`, 16 new tests pass. Merged into fuse e53b76a (P1 complete). Full suite: 544 passed, 1 failed: `test_telemetry::test_local_run_writes_a_run_summary` fails only when `tests/test_cli_init.py` (whole module) runs before it; passes alone and after each single init test. Dispatched as fix unit M2. |
| 10:4x | P4c | DeepSeek a14 | PASS 0778f94 after 1483 s, 39 steps, 2.8M tokens: bench/business_case.py, 10 tests, docs/business-case.md. Coordinator UAT: tests pass; `--seats 100 --json` gives break_even_month 74, units_needed 12 at published defaults (conservative: all tokens priced at decode rate). Merged into fuse. |
| 10:4x | P2a / M2 | DeepSeek a15 / GLM a16 | dispatched: P2a (loop through Registry) in worktree p2a; M2 (order-dependent telemetry test) in worktree m2 on GLM (first GLM try since the throttle). |
| 10:5x | P2b | DeepSeek a17 | dispatched (guard rules; context interface defined here, P2a plumbs it) in worktree p2b from c194dcf. Live: P4b a13 (29 edits), P2a a15, M2 a16 (GLM, no 1313 so far), P2b. |
| 10:5x | P4b | DeepSeek a13 | PASS 96b8db4 after 1888 s, 66 steps, 4.8M tokens: meeting-scribe TASK.md (261 lines), 41 hidden tests, 480-line reference, validate.sh. Coordinator UAT: validate.sh exit 0 (41 OK, skeleton fails). Decided forward: the brief's literal `unittest discover` command cannot run across sibling dirs; validate.sh uses bench/run.py's `_hidden_tests` mechanism instead. Merged into fuse. |
| 11:0x | P2c | DeepSeek a18 | dispatched (skill, install, MCP wrapper, README) in worktree p2c from 35a0d78. Live: P2a a15, M2 a16 (GLM), P2b a17, P2c a18 = 4 (cap). P3a waits on P2a; P3b on P3a; P4a on P3b. |
| 11:0x | M2 | GLM a16 | PASS 55c9e51 after 1191 s, 62 steps, 2.8M tokens. Root cause was in src, not the tests: `Telemetry.begin()` never guaranteed a first sample; `end()` racing it gave 0 samples 1500/1500 in a tight loop. Fix: synchronous first sample in `begin()`, replace-not-join stale samplers, regression test. Coordinator UAT: 27 passed ×3. Merged into fuse. GLM ran the whole unit without a 1313. |
| 11:1x | P2c | DeepSeek a18 | PASS d39f891 after 896 s, 66 steps, 4.6M tokens: SKILL.md on `subagent`, install.sh installs the package (SUBAGENT_SKIP_INSTALL guard for tests), MCP instructions from config + `lane=` warning, README rewritten for the fused tool with credits. Coordinator UAT: grep for delegate.py/.delegate empty, tests pass. Merged into fuse. Open: docs describe `.subagent/` + `[loop]` keys ahead of the loop internals (P2a lands them). |
| 11:1x | P2c | coordinator | merged 6b0af63 (the first merge attempt short-circuited on a grep that expected the literal "subagent run"; SKILL.md uses `DELEGATE run` with DELEGATE defined as `subagent`, accepted). |
| 11:1x | P3b / P4a | coordinator | worktrees p3b, p4a from 6b0af63. P3b (report + HTML) can build against the delegation record schema in the P3a brief with a hand-written fixture; P4a (bench harness parsers, matrix, dry-run) calls `subagent report` through a subprocess it stubs in tests. Dispatch: P3b DeepSeek, P4a GLM (M2 ran on GLM without a 1313). |
| 11:2x | P2b | DeepSeek a17 | PASS 8b598d9 after 1155 s, 47 steps, 4.3M tokens: `classify(context=)` with protected/state_allow rules, refs/subagent denial, 203 guard tests. Merged into fuse. Note for P2a: MultiEdit is a write tool only with a context; `git show refs/subagent/...` is denied too (literal token rule, as briefed). |
| 11:2x | P3b / P4a | DeepSeek a19 / GLM a20 | dispatched (report + dashboard; bench matrix). Live: P2a a15 (36 edits, 10 files), P3b, P4a. |
| 11:1x | P2a | DeepSeek a15 | FAILED `steps` (120) after 2056 s, 166 steps, 11M tokens: 10 files modified + loop/session.py, uncommitted, loop tests not collecting. Continued in the same session with an ordered recovery plan (commit WIP, get the target tests green, then finish, report by step 90). |
| 11:2x | P4a | GLM a20 | FAILED `steps` (80) after 1415 s, 94 steps, 6.4M tokens; harness.py, parsers with 5 fixtures, run.py matrix, concurrency.py move all on disk and 27 tests passing, nothing committed. Continued in the same session: commit, dry-run, report within 40 steps. No 1313 on this GLM run. |
| 11:3x | P4a | GLM a20 | PASS dd507ad after the continue (215 s, 8 steps): harness.py (5 parsers), run.py matrix with per-cell session_root, concurrency.py, docs/benchmark.md, 27 tests + 5 fixtures. Coordinator UAT: 64 tests pass, dry-run prints the cell. Merged into fuse. Open: bench/concurrency.py's engine scripts/lane_harness.py is stale against the new config (Settings.from_env gone); fixtures hand-written; live matrix pending P2a/P3a/P3b. |
| 11:4x | M3 | GLM a21 | dispatched: scripts/lane_harness.py, smoke_lanes.py, bench/concurrency.py onto core.InProcessServer (P4a finding). Live: P2a a15 (66 edits, 29 files), P3b a19, M3. |
| 11:5x | P3b | DeepSeek a19 | cancelled after 2400 s: 29 tool calls, all reads (runs.py, cost_join, greps), zero edits; not looping but far past the "edit by call 15" rule. Re-dispatched on DeepSeek (a22) with a 6-read budget and an ordered write plan (fixture → report.py → template → tests). |
| 11:5x | P2a | DeepSeek a15 | progress: 17bd467 "loop tests pass on both fakes" committed in the continued session. |
| 11:5x | M3 | GLM a21 | PASS ecc37d1 after 953 s, 40 steps, 1.4M tokens: lane_harness over core.InProcessServer, smoke_lanes/concurrency on `--provider` + `--config`. Coordinator UAT: 70 tests, both --help exit 0, grep empty. Merged into fuse. Open (recorded): scripts/smoke_guard.py, hook_isolation_check.py, reopen_lane.py still on the old SAM_/from_env surface. |
| 12:1x | P2a | DeepSeek a15 | PASS 9311fd7 after the continue (2748 s more, 119 steps; total 285 steps, 22M tokens): loop on Registry, `[loop]` TOML settings, cli cmd_* commands, fake_claude.py, test_loop_registry (adopt/continue, pre_verify ordering, guard_context reaches classify), loop tests on both fakes. Coordinator UAT: server verification 113 passed; greps clean; merged cleanly into fuse aa1ce40; full suite 717 passed. |
| 12:1x | P3a | DeepSeek a24 | dispatched (delegation trace kind, cost at run end) in worktree p3a from aa1ce40. P3b (a22) has committed its fixture (6752e62); M4 (a23, GLM) editing. |
| 12:2x | M4 | GLM a23 | PASS 3bf7e2b after 1059 s, 45 steps, 2.3M tokens: smoke_guard, hook_isolation_check, reopen_lane on the config-driven harness; 13 fake-server tests. Coordinator UAT: 57 tests, three --help exit 0, grep empty. Merged into fuse. Accepted judgement calls: reopen_lane has `--config` only; hook_isolation_check uses write_provider_config/load_settings rather than InProcessServer. |
| 12:3x | UAT 1.6 | coordinator | live `subagent doctor --provider glm --prompt` PASS through the fused tool (health ok, 16 output tokens, TTFT 5.5 s). grok: `refused (grok_balance)` — the 402 classification works; live grok DEFERRED (Grok Build balance exhausted). doctor --no-probe lists the three providers from the config. |
| 12:5x | P3b | DeepSeek a22 | PASS b103169 (4 commits) after 2032 s, 55 steps, 5.0M tokens on the second attempt: report.py, report_template.html, fixture trace, 6 tests, `report` CLI hook. Coordinator UAT: 6 tests pass; `subagent report --html` writes a 14.5 kB self-contained file, no external src. Merged into fuse. |
| 12:5x | UAT 2.6 | coordinator | first live loop attempt on cron returned `bad_outline` at once (my TESTS.md did not follow the outline format); fixing the outline and rerunning. |
| 2026-09-23 02:5x | P3a | DeepSeek a24 | implementation complete after 2245 s, 100 steps, 6.9M tokens; its `git commit` was denied "supervisor unavailable" (the guard's agent tier runs `claude -p`, which was failing while the coordinator's Claude quota was out — finding U-G1). Coordinator ran acceptance (18 targeted tests, 117 in the child's run), committed 97c7680 on its behalf, merged 38ce848; full suite 751 passed. |
| 03:0x | UAT 2.6 | coordinator | second live loop attempt: `test_writer_error` was `no provider took the run: glm unavailable` — my launch command had not sourced the key file; not a tool fault. Finding U-L1: the loop's JSON reports `test_writer_error` without the underlying provider error; it should carry it. Third attempt launched with keys. DeepSeek closed again 09:42 local (402). |
| 03:0x | lanes | Gary | "You can use Codex Luna 6 at xhigh and GLM as your subagents": ~/.codex/config.toml model set to gpt-5.6-luna (effort already xhigh; backup config.toml.bak-2026-09-23); codex lane probed. |
| 03:1x | codex | coordinator | codex lane probe PASS (28 s, gpt-5.6-luna at xhigh via the CLI config; guard sandbox+hook, verified). Finding U-S1: the MCP servers Claude Code runs are `~/.local/share/uv/tools/subagent-mcp` (a uv tool install of the pre-fusion package), not this checkout's venv; several sessions each run one. The hook shim in this checkout still covers them because their hook path points here. |
| 03:1x | UAT 2.6 | coordinator | third live loop attempt running: test writer on glm is active (`claude -p … writing a TEST SUITE from an outline`). |
| 03:1x | R.1 | Codex a2 | adversarial review dispatched in worktree review from 38ce848. |
| 03:3x | codex | Gary | "Can you call Codex Luna 6 xhigh?": both `gpt-6-luna` and `gpt-5.6-luna` answer at xhigh; ~/.codex/config.toml now `model = "gpt-6-luna"`, effort xhigh. The review already running (a2) booted on gpt-5.6-luna and is left to finish; every later Codex child uses gpt-6-luna. |
| 03:4x | R.1 | Codex a2 | cancelled at 490 s (Gary: "Luna 6, not 5.6"); it had reached the guard-probe stage (was testing `python -c` writes to tests/ and `.subagent/`, and `git update-ref refs/./subagent/x`); the last probe was a guard escalation. Re-dispatched as a3 on gpt-6-luna xhigh. |
| 03:4x | UAT 2.6 | coordinator | third loop attempt: test writer (glm) wrote tests/test_cron.py and verified; worker round on glm ran and its suite passed ("Ran 9 tests OK"); the review provider (deepseek) refused with `deepseek_balance`; `run --wait 540` returned `running` (exit 3) as documented; collecting with `subagent wait`. |
| 03:5x | UAT 4.4 | coordinator | one real bench cell launched: `bench/run.py --harness claude --config live/config.toml --tasks cron --modes delegate --runs 1` (claude -p orchestrates with the fused /delegate skill; glm workers; hidden tests after). Output under live/bench/. |
| 04:0x | UAT 2.6 | coordinator | PASS: the fused loop on the cron task ended `status: done` — test writer (glm) wrote 9 tests from the outline; round 1 hit the 900 s run_timeout in my config; round 2 (glm) passed all 9 tests; cross-family review on deepseek errored (balance) and was reported as `unverified`, not hidden. One `delegation` record: wall 1265 s, 41k output tokens, counterfactual $1.99, provider USD null (flat plan, spread at report time). Note: the worker left `__pycache__` and a `check_task.py` in the tree (changed_files). |
| 04:0x | UAT 3.3 | coordinator | `subagent report --trace <live trace> --html` on real data: provider and delegation tables print, dashboard written, no external src. Two follow-ups: U-R1 the provider table is keyed by vendor (`zai`) while the delegation table says `glm`; U-R2 `prov_usd` is null for the flat-plan provider although `[providers.glm.pricing] monthly_usd = 80` is set (spreading still reads the legacy pricing.toml path). |
| 04:3x | R.1 | Codex a3 (gpt-6-luna xhigh) | review written after 1448 s, 91 steps, 6.5M tokens: 2 HIGH (H1 undeclared secrets inherited by worker subprocesses; H2 provider names/delegation ids unescaped in the dashboard SVG), 11 MED, 1 LOW; verdict FAIL for unattended use until the HIGHs and state-integrity gaps close. Child could not commit (worktree gitdir under the parent's .git/worktrees is outside the Codex writable sandbox — finding U-C1) nor mkdir /tmp probes (guard); coordinator committed 7c31f17 and merged c293b00. Fix units F1a (Codex) and F1b (GLM) dispatched. |
| 05:0x | UAT 4.4 | coordinator | one real bench cell finished: claude -p orchestrating with the fused /delegate skill, glm workers, cron task → hidden tests 22/22 PASS on the produced code; results.csv/parquet/json, summary.md, providers.csv, delegations.csv and dashboard.html written. The orchestrator was cut by the bench's default 2400 s timeout, so orchestrator cost/tokens and the delegation totals are null in the row (the loop's rounds are in the cell trace). Rerun with `--timeout 5400` once GLM is free (F1b is on it). |
| 05:3x | F1a | Codex a4 (gpt-6-luna) | hit the codex lane's 1800 s run timeout with 16 files modified, uncommitted; its target tests: 270 passed, 1 failed (pre_verify test on the claude fake). Continued in the same session with an ordered recovery (fix, acceptance, outcomes, commit, report). |
| 05:4x | F1a | Codex a6 | a4 was reaped after its timeout (`continue` failed); fresh Codex agent dispatched on the same worktree to finish from the 16-file diff (one failing test, acceptance, outcomes, commit). Finding U-C2: a timed-out Codex agent cannot be continued; the run timeout should be per-unit configurable (`SAM_CODEX_RUN_TIMEOUT` is 1800 on this server). |
| 05:5x | F1b | GLM a5 | hit the 80-step cap after 2079 s, 92 steps, 11.6M tokens: 14 files modified, uncommitted; acceptance 125 passed, 1 failed (flat-plan pricing from the cell config in bench). Continued in the same session with an ordered recovery. |
| 06:2x | F1b | GLM a5 | PASS 69ea559 after the continue (722 s, 24 steps): nine findings fixed with tests (copilot loop flag, missing-key warning, setup runs in delegation totals, flat-plan spread into totals from the TOML PricingSpec, bench cell-id config tag, --from-report fields, undersized-fleet ROI, report keyed by provider name + vendor column, prov_usd from config). Coordinator UAT: 126 targeted tests; report shows prov_usd 80.00 for the flat plan. Merged into fuse. |
| 06:5x | UAT 4.4 | coordinator | PASS with numbers: bench cell `claude-config-0367aa72-cron-delegate-1` (claude -p orchestrating the fused /delegate skill, glm workers): hidden tests 22/22, orchestrator $0.68 in 11 turns, worker glm 70,259 in / 39,062 out / 1,019,328 cache-read tokens, counterfactual $1.84, 1 round, 1 review, verified True, wall 1065 s; results.csv/parquet/json, summary.md, dashboard.html written. Follow-ups: U-B1 `orch_tokens_*` columns empty although `orch_cost_usd` parsed (claude parser maps cost but not modelUsage token sums); the flat-plan `worker_usd` shows the whole $80 month on the only run of the month (correct by policy, misleading in a single-cell view). |
| 06:5x | F1a | Codex a6 | finisher reported FAIL-in-sandbox: the Codex sandbox cannot bind Unix/TCP sockets, so every server-backed test failed inside it, while the server's own verification outside passed (297). Finding U-C3. Coordinator ran the full suite on the worktree outside the sandbox: 37 failed / 724 passed (allowlist drops variables the fakes and codex/grok env tests rely on). Fix unit F1a-2 dispatched on Codex (a7). |
| 07:1x | README | GLM a8 | PASS fd70185 after 368 s, 15 steps: README "First measured cell (2026-09-23)" section, docs/benchmark.md "Measured cells", wiki/data/bench-2026-09-23/README.md; every number checked against results.csv/summary.md. Coordinator committed the five data files and merged into fuse. |
| 07:2x | UAT 4.4 (codex) | coordinator | second-harness cell launched: `bench/run.py --harness codex` (gpt-6-luna xhigh orchestrating, glm workers, cron). Prerequisites the bench does not yet install (finding U-B2): the delegate skill linked for Codex at `~/.agents/skills/delegate` → repo skills/delegate, and the repo's `.venv/bin` on PATH so the orchestrator can run `subagent`. Grok as orchestrator DEFERRED (balance exhausted); Gemini not installed. |
| 07:3x | F1a | Codex a7 | second Codex pass brought the full-suite failures from 37 to 18 (all in the loop's `--wait` respawn path: autopilot, outline fix-pass, delegation trace) but could not run socket-backed tests in its sandbox and could not commit (worktree gitdir). Coordinator committed WIP 223ed5a. Fork decided: GLM is carrying the codex-orchestrator bench cell (one GLM child at a time), DeepSeek is closed, Codex cannot verify socket tests → a Claude agent finishes F1a (lane plan fallback). |
| 07:4x | UAT 4.4 (codex) | coordinator | codex-orchestrator cell ended in 289 s with hidden 0/1: gpt-6-luna read the skill, wrote PLAN.md and TESTS.md, then every `subagent` call died with `approval socket did not start: PermissionError` — the Codex `workspace-write` sandbox cannot bind Unix sockets (U-C3 again), and even `subagent detect` starts the approval server (finding U-L2). Orchestrator tokens were parsed (36,565 in / 7,574 out / 401,152 cached). Fix unit M5 on GLM: bench harness runs codex orchestrators with `-s danger-full-access` (throwaway cell), installs the delegate skill into the cell, puts the venv on PATH; read-only loop commands stop starting the server. |
| 2026-09-24 00:1x | F1a | Claude agent | PASS 0b404cb + merge a652a37: root cause of the 18 loop failures was fix (4), adopt's live-id check firing on the loop's own reopened agents; adopt now evicts a closed agent with terminal runs, run_round closes the worker after writing its session record; the five review fixes intact. Coordinator UAT: 775 passed on the worktree, 14 "Outcome: fixed". Fast-forwarded into fuse. |
| 00:1x | M5 | GLM a9 | PASS b50c36b after 1268 s, 61 steps: codex orchestrator cells run `-s danger-full-access`, grok `--sandbox off`; the bench copies the delegate skill into the cell and prepends the venv bin to PATH (recorded in cell.json); `detect/test/undo/checkpoints/watch` run without the approval server. Coordinator UAT: 54 targeted tests, dry-run shows the flag. Merged eaca55f; full suite 799 passed. |
| 00:2x | UAT 4.4 (codex) | coordinator | codex-orchestrator cell relaunched on the fixed harness (bench4). |
| 00:3x | UAT | coordinator | goal rows audited on fuse eaca55f+: 0.1–0.5, 1.2–1.5, 2.1, 2.3, 2.5, 3.1–3.3, 4.1–4.3, R.1 (both HIGH fixed; two open findings carry reasons), R.3 (nothing pushed) PASS. Row 1.1 FAIL as written: `require_providers()` exists but the CLI never calls it (`doctor --no-probe` on an empty config exits 0 with an empty table). Fix unit M6 on Codex. |
| 00:5x | M6 | Codex a10 | PASS 61114ac (committed by the coordinator; sandbox denied git add): every entry point except `init` calls `require_providers()`; `doctor --no-probe` on an empty config now exits 1 with "no providers configured". Row 1.1 PASS. Fast-forwarded; full suite 805 passed. |
| 01:5x | UAT 4.4 (codex) | coordinator | second-harness cell PASS on the product: Codex gpt-6-luna orchestrating the fused skill delivered code that passes hidden tests 22/22 in 4988 s; orchestrator tokens parsed (193,422 in / 40,675 out / 10,768,128 cached). Worker accounting missing (finding U-B4): the orchestrator re-issued `subagent run … --wait 540` four times (each a new run id, `running`) instead of `subagent wait`; `--tier hard` named the closed DeepSeek provider; no delegation record under the cell's session root. Data in `wiki/data/bench-2026-09-24-codex/`. |
| 01:5x | close | coordinator | UAT audit against `goals/2026-09-22-fuse-delegation.md`: 0.1–0.5 PASS; 1.1–1.5 PASS; 1.6 glm PASS, grok DEFERRED (balance); 2.1–2.5 PASS; 2.6 PASS (loop `done` on cron); 3.1–3.3 PASS; 4.1–4.3 PASS; 4.4 PASS for claude and codex harnesses (grok DEFERRED balance, gemini/copilot not installed; self-hosted providers not measured: bppc down, oMLX no model loaded); R.1 PASS (both HIGH fixed, two open findings with reasons); R.2 PASS; R.3 PASS (nothing pushed). |

### Findings (U-*)

- U-T2 `transcript` shows no activity while a run is live (351 lines appeared only at cancel).
- U-R1 a Codex usage-limit refusal on the first turn was classified `usage_limit_after_work` (fixed in P1a).
- U-H1 the server's hook path and env names were bound to the checkout it started from; a layout change silently failed every child call closed until a shim was added.
- U-D1 deepseek-v4-pro spent 47 minutes re-reading files without an edit on P1a; the loop killer fired.
- U-G1 the guard's `agent` supervisor tier runs `claude -p`, so it fails closed when the coordinator's Claude quota is out (blocked a child's git commit).
- U-S1 the MCP servers Claude Code runs are a uv tool install of the pre-fusion package, not this checkout's venv.
- U-L1 the loop reports `test_writer_error` without the provider's own error text.
- U-L2 `subagent detect` started the approval server (fixed in M5).
- U-C1 a Codex child cannot `git add` in a worktree (gitdir under the parent's .git/worktrees is outside its sandbox); U-C2 a timed-out Codex agent cannot be continued; U-C3 the Codex sandbox cannot bind sockets, so socket-backed tests and the loop's approval socket fail inside it (bench cells use `-s danger-full-access` since M5).
- U-B1 the claude bench parser fills cost but not `orch_tokens_*`; U-B2 the bench did not install the skill or PATH for non-Claude orchestrators (fixed in M5); U-B4 the Codex-orchestrated cell has no worker accounting (see `data/bench-2026-09-24-codex/README.md`).
